# Token simples do teu app pessoal. Use uma frase/senha grande.
APP_SECRET=troque-por-uma-senha-grande

# Em dev, o Vite roda em 5173. Em produção use a URL real do app.
CORS_ORIGIN=http://localhost:5173
PORT=3001

# Ordem de fallback. O backend tenta na ordem até algum provedor gerar o áudio.
# Exemplos:
# TTS_PROVIDER_ORDER=elevenlabs,google,openai
# TTS_PROVIDER_ORDER=google,elevenlabs,openai
# TTS_PROVIDER_ORDER=google
TTS_PROVIDER_ORDER=elevenlabs,google,openai

# ElevenLabs: pode usar uma ou várias chaves separadas por vírgula.
# ELEVENLABS_VOICE_IDS acompanha a ordem das chaves; se só houver uma voz, ela será usada para todas as chaves.
ELEVENLABS_API_KEYS=coloque_key_1,coloque_key_2
ELEVENLABS_VOICE_IDS=coloque_voice_id_1,coloque_voice_id_2
ELEVENLABS_MODEL_ID=eleven_multilingual_v2
ELEVENLABS_OUTPUT_FORMAT=mp3_44100_128

# Google Cloud TTS.
# Preferência: cole o JSON da service account em base64 para evitar quebra de linha no Render.
# Windows PowerShell: [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes((Get-Content .\service-account.json -Raw)))
# Linux/macOS: base64 -w 0 service-account.json
GOOGLE_TTS_CREDENTIALS_B64=cole_o_json_da_service_account_em_base64
# Alternativa: GOOGLE_TTS_CREDENTIALS_JSON={"type":"service_account",...}
GOOGLE_TTS_VOICES=pt-BR-Wavenet-A,pt-BR-Wavenet-B
GOOGLE_TTS_LANGUAGE_CODE=pt-BR
GOOGLE_TTS_AUDIO_ENCODING=MP3
GOOGLE_TTS_SPEAKING_RATE=1
GOOGLE_TTS_PITCH=0

# OpenAI: pode usar uma ou várias chaves separadas por vírgula.
OPENAI_API_KEYS=sk-proj-chave_1,sk-proj-chave_2
TTS_MODEL=gpt-4o-mini-tts
TTS_VOICE=marin
